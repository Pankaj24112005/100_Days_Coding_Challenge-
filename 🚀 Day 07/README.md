# Day 7 – File Handling in Python 🧠🐍

File handling is an essential concept in Python that allows programs to read data from files and write data to files. It is widely used in real-world applications such as data storage, logging systems, and automation scripts.

---

## 📌 Topics Covered

- What is file handling and why it is important  
- Opening files using `open()`  
- File modes:
  - `r` – Read
  - `w` – Write
  - `a` – Append
  - `x` – Create
- Reading files:
  - `read()`
  - `readline()`
  - `readlines()`
- Writing and appending data to files  
- Closing files properly  
- Using the `with` statement for safe file handling  
- Basic exception handling (`FileNotFoundError`)

---

## 📂 Files in This Folder

- `07_file_handeling.py` – Python program demonstrating file handling operations

---

## 🚀 How to Run

1. Make sure Python is installed on your system  
2. Open a terminal or command prompt  
3. Run the file using:
   ```bash
   python 07_file_handeling.py
